export const ROOT_CONTROLLER_NAME 	 = 'rootController';
export const ROOT_CONTROLLER_AS_NAME = 'rootCtrl';

class rootController {
	constructor(){
    // nothing for now
	}
}

rootController.$inject = [];

export default rootController;
